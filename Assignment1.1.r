#Example of Recycling of Vectors
#Assigning values 1,2,3 to vector x
x <- c(1,2,3)
x
#Assigning values 2,3 to vector y
y <- c(2,3)
y
#Adding the two vectors, i.e. x and y
x+y
#The shorter vector was recycled so the vector y value was converted to 
# y <- c(2,3,2) (Recycled Value)